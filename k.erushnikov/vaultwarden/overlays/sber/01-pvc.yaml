apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaultwarden-data
spec:
  accessModes:
    - ReadWriteOnce
  nodeSelector:
    kubernetes.io/hostname: k.erushnikov.ru
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi